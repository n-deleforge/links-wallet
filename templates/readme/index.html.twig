{% extends 'base.html.twig' %}

{% block body %}
	<div class="container">
		<h1>{{ "readme.title"|trans }}</h1>

		<div class="p-3 bg-light">
			<h2>{{ "readme.actionMenu"|trans }}</h2>
			<div class="py-2">
				<a class="btn btn-primary" href="{{ path('app_readme_add') }}" role="button">{{ "readme.addLink"|trans }}</a>
				<a class="btn btn-primary" href="{{ path('app_view_readme', {username: app.user.name}) }}" role="button">{{ "readme.seeReadme"|trans }}</a>
			</div>
		</div>

		<div class="p-3 bg-light mt-3">
			<h2>{{ "readme.listMenu"|trans }}</h2>

			{% if not links %}
				<p>{{ "readme.noLinks"|trans }}</p>
			{% else %}
				<table class="table table-hover">
					<thead>
						<tr>
							<th scope="col">{{ 'readme.model'|trans }}</th>
							<th scope="col">{{ 'readme.username'|trans }}</th>
							<th scope="col">{{ 'readme.actions'|trans }}</th>
						</tr>
					</thead>
					<tbody>
						{%for link in links%}
							<tr>
								<td><i class="{{ link.model.icon }}"></i> {{ link.model.name }}</td>
								<td>{{ link.username }}</td>
								<td>
									{{ 'main.modify'|trans }}
									<a href="{{ path('app_readme_delete', { id:link.id }) }}">{{ 'main.delete'|trans }}</a>
								</td>
							</tr>
						{% endfor %}
					</tbody>
				</table>
			{% endif %}

		</div>
	</div>
{% endblock %}